{
  "tasks": {
    "run": "deno run --allow-net --allow-env proxy.ts",
    "dev": "deno run --allow-net --allow-env --watch proxy.ts",
    "check": "deno check proxy.ts",
    "compile": "deno compile --allow-net --allow-env --output ics-outlook-proxy proxy.ts"
  }
}
